/**
 * Регулярное выражение для валидации идентификаторов/номеров.
 * Разрешает:
 * - Буквы всех алфавитов (\p{L})
 * - Цифры (\d)
 * - Тире (\-)
 * - Слеш (\/)
 * Ограничение длины: от 1 до 20 символов.
 */
export const DOCUMENT_NUMBER_REGEX = /^[\p{L}\d\/\-]{1,20}$/u;
